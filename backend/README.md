# /app/backend — Deprecated (Emergent preview leftover)

**Not part of the portable project.**

The initial Emergent scaffold shipped a FastAPI + MongoDB backend at this
path. It is **not used** by the real application, which is 100% powered by
**Supabase** (schema in `../supabase/`, edge functions in
`../supabase/functions/`).

You can safely delete this folder before publishing the repository.
